import 'package:flutter/material.dart';

class ParamScreen extends StatelessWidget {
  const ParamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Param screen'));
  }
}
